import { YouTubeImagePipe } from './you-tube-image.pipe';

describe('YouTubeImagePipe', () => {
  it('create an instance', () => {
    const pipe = new YouTubeImagePipe();
    expect(pipe).toBeTruthy();
  });
});
